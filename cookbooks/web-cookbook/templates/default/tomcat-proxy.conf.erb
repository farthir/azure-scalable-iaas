server {
  listen 80;
  server_name <%= node['hostname'].byteslice(0, 9) %>.northeurope.cloudapp.azure.com;
  root /var/www/;

  location / {
    proxy_pass http://<%= node['web-cookbook']['app-lb-ip'] %>:8080/;
  }

  location ~* \.(gif|png|css|html)$ {
   expires 30d;
  }
}